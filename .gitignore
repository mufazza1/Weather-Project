# Generated data & logs (produced at runtime — do not commit)
*.tsv
*.log
weather_readings.tsv
historical_fc_accuracy.tsv

# Downloaded API responses at the project root
/wttr.json

# Test fixtures are committed even though they are JSON; nothing to ignore there.

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
